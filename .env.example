# Required when generation_provider is "gemini_api" in config.yaml (default)
GEMINI_API_KEY=

# Optional: needed when generation_provider is "openrouter" in config.yaml
OPENROUTER_API_KEY=

# Optional: needed when generation_provider is "hf_inference_api"
HF_API_TOKEN=
